import React from 'react';

export class Home extends React.Component {
  render() {
    const element = (
      <div>Home page</div>
    );

    return (
      element
    );
  }
}

export default Home;
